---
interface Props {
	iclass: string
	body: string
}

const { iclass, body } = Astro.props
---

<div
	class="mt-20 flex w-3/4 flex-col items-center justify-center rounded-2xl bg-Orange p-10 lg:h-64"
>
	<i class={iclass}></i>
	<p class="font-Poppins text-2xl font-light">{body}</p>
</div>
